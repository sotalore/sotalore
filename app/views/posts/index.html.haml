- page_title('Posts')

= layout_main_content do
  .flex.items-center
    %h2.m-2.grow Posts
    .shrink
      - if policy(Post).new?
        = new_button_to 'New Post', new_post_path

  - @posts.each do |post|
    = render partial: 'post', locals: { post: post, truncate: true }
