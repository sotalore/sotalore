= single_column do
  = tile do
    = tile_body do
      %h2
        New Brittania Time:
        %span{data: {controller: 'time'}}

  = tile do
    = tile_body do
      %h2{data: {controller: 'orbit', 'orbit-period-value': Clock::DAEDALUS_ORBITAL_PERIOD}}
        Daedalus Position:
        %small.font-normal
          %span{data: {'orbit-target': 'position'}}
          %span.text-sm.italic{data: {'orbit-target': 'note'}}

  = tile do
    = tile_body do
      %h2
        Planet Positions:

      %table.Table
        %thead.border-b-2.border-grey-300
          %th Planet
          %th Position
          %th Visible?
          %th Color
          %th Orbital Period
          %th Cabalist

        - Clock::PLANETS.each do |planet, info|
          %tr.hover:bg-grey-100{data: {controller: 'orbit', 'orbit-period-value': info[:orbital_period]}}
            %td.font-bold
              = planet.to_s.titleize
            %td
              %span.mr-4{data: {'orbit-target': 'position'}}
            %td
              %span.text-right.text-sm.italic{data: {'orbit-target': 'note'}}
            %td{class: "planet-#{info[:color]}"}
              = info[:color]
            %td
              = info[:orbital_period]
              days
            %td= info[:cabalist]

  = tile do
    = tile_body do
      %h2
        Constellations:

      .grid.grid-cols-4.gap-2
        - Constellations::ALL.each do |constellation, info|
          .text-right.font-bold.pr-2
            = constellation.to_s.titleize

          %div{data: {controller: 'constellation', 'constellation-offset-value': info[:offset]}}
          %div= info[:virtue]
          %div= info[:city]
