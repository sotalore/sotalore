= sl_form_for(avatar) do |f|
  = f.text_field :name
  = f.check_box :is_default, label: 'Set as default Avatar (for skills calculator)'
  = f.actions do
    - if avatar.persisted?
      = f.cancel('Cancel', avatars_path)
    = f.submit
